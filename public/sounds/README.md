# Custom game sounds

Add your own MP3 files to this folder with these exact names:

- `correct.mp3` plays after a swipe up.
- `skip.mp3` plays after a swipe down.
- `finish.mp3` plays when the round timer reaches zero.

The files are preloaded when a round starts. Keep them short so the controls stay responsive.